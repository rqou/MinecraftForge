--- ../src_base/common/net/minecraft/src/BlockDispenser.java
+++ ../src_work/common/net/minecraft/src/BlockDispenser.java
@@ -5,6 +5,8 @@
 import cpw.mods.fml.common.registry.GameRegistry;
 
 import java.util.Random;
+
+import net.minecraftforge.common.ForgeDirection;
 
 public class BlockDispenser extends BlockContainer
 {
@@ -499,4 +501,19 @@
             }
         }
     }
+    
+    @Override
+    public boolean rotateY(World world, int x, int y, int z, int amount)
+    {
+        boolean rotateNeg = amount < 0;
+        amount = Math.abs(amount) % 4;
+        if(rotateNeg)
+            amount = 4 - amount;
+        int oldMeta = world.getBlockMetadata(x, y, z);
+        ForgeDirection dir = ForgeDirection.getOrientation(oldMeta);
+        for(int i = 0; i < amount; i++)
+            dir = dir.getRotCcwY();
+        world.setBlockMetadataWithNotify(x, y, z, dir.ordinal());
+        return true;
+    }
 }
