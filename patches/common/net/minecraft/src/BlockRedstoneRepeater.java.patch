--- ../src_base/common/net/minecraft/src/BlockRedstoneRepeater.java
+++ ../src_work/common/net/minecraft/src/BlockRedstoneRepeater.java
@@ -328,4 +328,21 @@
     {
         return Item.redstoneRepeater.shiftedIndex;
     }
+    
+    @Override
+    public boolean rotateY(World world, int x, int y, int z, int amount)
+    {
+        amount = -amount;
+        boolean rotateNeg = amount < 0;
+        amount = Math.abs(amount) % 4;
+        if(rotateNeg)
+            amount = 4 - amount;
+        int oldMeta = world.getBlockMetadata(x, y, z);
+        int dir = oldMeta & 3;
+        int delay = (oldMeta & 12) >> 2;
+        for(int i = 0; i < amount; i++)
+            dir = (dir + 1) % 4;
+        world.setBlockMetadataWithNotify(x, y, z, dir | (delay << 2));
+        return true;
+    }
 }
